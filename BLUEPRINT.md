# Beaver Add-in Blueprint

This file is the canonical project blueprint for AI agents working in this repo.
Read this first for stable project knowledge, architecture, invariants, and file
ownership. For the actual work process and response style, see `GEMINI.md`.

Last updated: 2026-05-15
Current config version: 2.0.68

## Purpose

Beaver is an Excel VBA add-in for fast workbook operations, lightweight
automation, and repeatable spreadsheet workflows.

Core capabilities:

- range formatting and paste-format helpers
- formula manipulation and formula-to-value conversion
- text cleanup and date normalization
- workbook duplication and range export
- link breaking
- a manifest-driven Ribbon and hotkey system
- worksheet UDFs: `XFilter` and `XUnpivot`
- a lightweight VBA test harness
- a command-oriented execution pipeline built around `ICommand`

## Source Of Truth

Treat the exported text files in this repo as authoritative:

- `Modules\**\*.bas` and `Modules\**\*.cls` are the editable VBA source.
- `ThisWorkbook.cls` is the authoritative workbook event module on disk.
- `features.json` is the authoritative feature manifest for Ribbon controls,
  command names, icons, release tiers, runtime-test modes, and hotkeys.
- `config.json` is the authoritative configuration for add-in identity and UI
  constants. `Update.ps1` synchronizes its `Hotkeys`, `Icons`, and
  `FeatureFlags` sections, including `GeneratedFeatureCount`, from
  `features.json`.
- `ribbon.xml` is generated from `features.json` by `Update.ps1`.
- `Modules\Infrastructure\Infra_CommandRegistry.bas` is generated from
  `features.json` by `Update.ps1`.
- `Beaver Add-in.xlsm` is the compiled workbook artifact synchronized by
  `Update.ps1`.

Do not make manual changes in the VBE and leave disk exports stale. Edit the
text files first, then run `.\Update.ps1`.

## Do Not Touch

- `Beaver Add-in.xlsm` directly. Always use `Update.ps1`.
- `Modules\Libraries\Lib_JsonConverter.bas`. It is a third-party library.
- `Update.ps1` unless the build pipeline itself is the task.
- `.frx` files. They are binary and auto-generated by the VBE.
- hidden workbook support sheets such as `_BeaverUndo` or `_BeaverTests`, if
  they exist.

## Project Layout

```text
Excel Add-in\
|- Beaver Add-in.xlsm
|- ThisWorkbook.cls
|- ribbon.xml
|- features.json
|- config.json
|- Update.ps1
|- BLUEPRINT.md
|- GEMINI.md
\- Modules\
   |- Commands\
   |  |- FeatCmd_*.cls         ICommand feature implementations
   |  \- Feat_*.bas            legacy or thin feature entry modules
   |- Core\
   |  |- AppContainer.cls      DI / service locator and command resolver
   |  \- ICommand.cls          command execution interfaces
   |- Infrastructure\
   |  \- Infra_*.bas/.cls      infrastructure, config, diagnostics, typed requests/contexts
   |- Libraries\
   |  \- Lib_*.bas             libraries, UDFs, and test manifest
   \- UI\
      |- UI_Ribbon.bas         Ribbon callbacks
      \- UI_Hotkeys.bas        hotkey callbacks
```

## Runtime Architecture

The current architecture is command-oriented rather than direct macro-to-feature
wiring.

### Entry Paths

1. Workbook lifecycle:
   `ThisWorkbook.Workbook_Open` calls `Infra_Bootstrap.Startup`.
   `ThisWorkbook.Workbook_BeforeClose` calls `Infra_Bootstrap.Shutdown`.

2. Ribbon callbacks:
   `UI_Ribbon.bas` exposes `Ribbon_On*` procedures referenced by
   `features.json` and `ribbon.xml`.
   These callbacks do not perform heavy business logic directly. They call
   `AppContainer.ExecuteEntryPoint`.

3. Hotkeys:
   `Infra_Hotkeys` registers `Application.OnKey` bindings from `config.json`.
   Those bindings point to `UI_Hotkeys.bas`, and `UI_Hotkeys` forwards to
   `AppContainer.ExecuteEntryPoint`.

### Bootstrap And Container

- `Infra_Bootstrap.Startup` initializes `AppContainer` with `Infra_Config`,
  `Infra_Error`, and `StateStore`, then registers hotkeys.
- `Infra_Bootstrap.Shutdown` unregisters hotkeys.
- `Infra_Bootstrap.EnsureStarted` ensures container initialization for late
  entry scenarios.
- `AppContainer` is the central dependency container and command resolver.
- `AppContainer.ResolveCommand` delegates to `Infra_CommandRegistry` to map
  manifest command names such as `Wrap` to `FeatCmd_*` classes.
- `AppContainer.CreateCommandContext` builds a `BasicCommandContext` with entry
  macro, source, trigger kind, and captured Excel action context.

### Command Pipeline

- `features.json` and hotkey metadata define `CommandName` values.
- `Infra_CommandRegistry` is generated from `features.json` and maps entry
  macros to command names while also creating `FeatCmd_*` instances by
  `CommandName`.
- `AppContainer.ExecuteEntryPoint` resolves the command name from the callback
  macro and creates the command object.
- `CommandInvoker.ExecuteCommand` runs `CanExecute`, applies
  `Infra_AppStateGuard`, calls `Execute`, and registers pending Undo actions.
- Shared helpers in `Infra_CommandSupport` convert `ICommandContext` into
  `Infra_ActionContext`.

### Shared Infrastructure

- `Infra_AppStateGuard` captures and restores Excel application state such as
  `ScreenUpdating`, `EnableEvents`, `DisplayAlerts`, and `Calculation`.
- `Infra_AppState` captures workbook, worksheet, cell, and range context and
  provides selection helpers.
- `Infra_Config` loads `config.json` through the typed `Infra_ConfigModel`.
- `Infra_Error` centralizes tracking, breadcrumbs, diagnostics, user-facing
  error handling, and failsafe reset behavior.
- `Infra_ContextTracker` provides RAII-style `PushContext` and `PopContext`
  support via `Infra_Error.Track`.
- `Infra_Interaction` centralizes message boxes, text prompts, range prompts,
  and reusable `UserForm`-based option pickers.
- `Infra_Undo` provides custom undo support for macro-driven changes.
- `Infra_Progress` provides status-bar progress reporting.
- `Infra_Diagnostics` and `Infra_ErrorContext` support environment capture and
  troubleshooting.
- `StateStore` and `AppContainer` provide shared process-level state and
  dependency access.

## Current Feature Surface

### Ribbon-backed commands

- `Wrap` -> `FeatCmd_Wrap` (Supports formula reuse and pattern typing)
- `StaticSheetWorkbook` -> `FeatCmd_StaticSheetWorkbook`
- `CleanData` -> `FeatCmd_CleanData`
- `BreakExternalLinks` -> `FeatCmd_BreakExternalLinks`
- `Duplicate` -> `FeatCmd_Duplicate`
- `ExportImageOrPdf` -> `FeatCmd_ExportImageOrPdf`
- `ShowHelpCenter` -> `FeatCmd_ShowHelpCenter`

### Hotkey-driven commands

- `ApplyCustomNumberFormat` -> `FeatCmd_ApplyCustomNumberFormat`
- `MakePermanent` -> `FeatCmd_MakePermanent`
- `CreateSheet` -> `FeatCmd_CreateSheet`
- `FillDown` -> `FeatCmd_FillDown`
- `FilterByCell` -> `FeatCmd_FilterByCell`
- `PasteFormat` -> `FeatCmd_PasteFormat`
- `FormatRange` -> `FeatCmd_FormatRange`
- `Backspace` -> `FeatCmd_Backspace`
- `Delete` -> `FeatCmd_Delete`

### UDFs

- `Lib_XFilterFunction.XFilter`
- `Lib_XUnpivotFunction.XUnpivot`

### Testing

- `Lib_Tests.RunAllTests`
- `Modules\Libraries\Lib_TestManifest.bas` is generated from `Public Sub Test_*`
  procedures.
- Additional test suites currently include
  `Lib_Tests_CommandInfrastructure.bas`.

## Current Ribbon And Hotkey Contract

### Ribbon groups

- `Formatting`: wrap (mode picked in a `UserForm`), static sheet/workbook
- `Data Tools`: clean data, break links
- `File Actions`: duplicate workbook, export range
- `Support`: help center

Interactive command notes:

- fixed-choice flows such as Clean Data scope, Export format, Make Static
  scope, Break Links scope, Wrap mode, and sheet placement now use a reusable
  `UserForm` picker instead of typed text entry
- free-form inputs such as formula patterns still use text prompts
- save destinations for duplicate/export flows use a shared Save As picker with
  Desktop-based defaults and overwrite confirmation

### Ribbon controls currently in `features.json`

- `BtnWrap`
- `BtnStaticSheetWorkbook`
- `BtnCleanData`
- `BtnBreakLinks`
- `BtnDuplicate`
- `BtnExport`
- `BtnHelpCenter`

### Hotkeys currently in `config.json`

- `^+4` -> `UI_Hotkeys.Hotkey_ApplyCustomNumberFormat`
- `^+p` -> `UI_Hotkeys.Hotkey_MakePermanent`
- `+{F11}` -> `UI_Hotkeys.Hotkey_CreateNamedSheet`
- `^%{DOWN}` -> `UI_Hotkeys.Hotkey_FillDown`
- `^+f` -> `UI_Hotkeys.Hotkey_FilterBySelectedCell`
- `^+m` -> `UI_Hotkeys.Hotkey_PasteFormat`
- `^+q` -> `UI_Hotkeys.Hotkey_FormatSelectedRange`
- `{BACKSPACE}` -> `UI_Hotkeys.Hotkey_Backspace`
- `{DELETE}` -> `UI_Hotkeys.Hotkey_Delete`

If you add or change a shortcut, update `features.json` and let `Update.ps1`
sync it into `config.json`. Do not hardcode new `Application.OnKey` bindings
inside feature modules.

## Config Contract

`config.json` currently contains:

- `UIConstants`
- `AddinIdentity`
- `FeatureFlags`
- `Hotkeys`
- `Icons`

### Schema snapshot

```json
{
  "UIConstants": {
    "DefaultFontName": "Calibri",
    "DefaultFontSize": 10,
    "HeaderFontSize": 11,
    "DefaultNumberFormat": "#,##0",
    "DefaultDateFormat": "dd-mmm-yyyy",
    "DisplayDateFormat": "dd/mm/yyyy",
    "ColumnWidthThreshold": 40,
    "MaxColumnWidth": 25,
    "HeaderColor": "#AEAAAA",
    "DefaultExportScale": 3,
    "MaxExportScale": 10
  },
  "AddinIdentity": {
    "Name": "Beaver Add-in",
    "Version": "2.0.xx"
  },
  "FeatureFlags": {
    "ManifestFile": "features.json",
    "GeneratedFeatureCount": 8
  },
  "Icons": {
    "BtnCleanData": "Clear"
  },
  "Hotkeys": [
    {
      "Key": "^+p",
      "Macro": "UI_Hotkeys.Hotkey_MakePermanent",
      "CommandName": "MakePermanent",
      "Description": "Make selection permanent (values)"
    }
  ]
}
```

Notes:

- `ReleaseTier` and `IncludeDevFeatures` are still supported by
  `Infra_ConfigModel`/`Infra_Config`, but they may be omitted from the checked-in
  `config.json` and fall back to defaults during load.
- `GeneratedFeatureCount` is synchronized from the enabled manifest features, so
  treat the exact number as generated state rather than hand-maintained config.

If a new shared setting is needed:

1. Add it to `config.json`.
2. Update `Infra_ConfigModel`.
3. Expose it via `Infra_Config`.
4. Use the typed accessor instead of scattering literals.

For stable feature flags, prefer first-class typed properties on
`Infra_ConfigModel` and `Infra_Config`. Keep `FeatureFlags` as the fallback
bucket for dynamic or low-frequency metadata.

## VBA Rules

- `Option Explicit` is mandatory in every `.bas` and `.cls` file.
- Every VBA source file should include the metadata header:

```vba
' @Module: ModuleName
' @Category: Infrastructure/Feature/UI/Library
' @Description: Short description
' @ManagedBy: BeaverAddin Agent
' @Dependencies: Dependency list
```

- Keep UI callbacks and workbook events thin.
- Prefer command classes (`FeatCmd_*`) for new feature work.
- If implementing `ICommand`, include both `CanExecute` and `Execute`.
- Use `Infra_CommandSupport.ActionContextFromCommandContext` or captured
  `Infra_ActionContext` instead of re-reading globals repeatedly.
- Prefer array-based range processing over cell-by-cell loops for bulk edits.
- Use `.Formula2` for modern formulas and spill-safe behavior.
- Preserve user-facing constants in `config.json`.
- Do not manually edit generated test manifests or generated Ribbon output
  unless the task specifically targets the generator/build path.

## Error Handling Pattern

Public entry points, UI callbacks, and lifecycle hooks should follow the
centralized error pattern:

```vba
Public Sub MyMacro()
    Dim tracker As Object: Set tracker = Infra_Error.Track("MyMacro")
    Dim guard As New Infra_AppStateGuard
    On Error GoTo ErrHandler

    ' work

CleanExit:
    Exit Sub

ErrHandler:
    Infra_Error.HandleError "MyMacro", Err
    Resume CleanExit
End Sub
```

Rules:

- Use `Infra_Error.Track` at the start of every public macro, Ribbon callback,
  hotkey wrapper, and workbook event.
- Use `Infra_AppStateGuard` for code that changes Excel application state.
- Use `Infra_Undo.SaveState` before mutating ranges when custom undo should be
  preserved. Registration is automatically delayed until the command finishes
  via the `CommandInvoker` pipeline.
- Use `Infra_Progress` for operations that may take noticeable time.
- Prefer a single `CleanExit` label when there are early returns.
- Route failures through `Infra_Error.HandleError` instead of ad hoc dialogs.

## Manifest And UI Rules

- Prefer editing `features.json` over hand-editing `ribbon.xml`.
- Keep `features.json`, `config.json`, `ribbon.xml`, `UI_Ribbon.bas`,
  `UI_Hotkeys.bas`, and generated `Infra_CommandRegistry` aligned.
- A new Ribbon feature usually needs:
  - a `Features` entry in `features.json`
  - a matching `Ribbon_On*` callback in `UI_Ribbon.bas`
  - a `CommandName`
  - a `FeatCmd_*` implementation
  - a regenerated `Infra_CommandRegistry`
  - an icon entry synced into `config.json`
- A new hotkey usually needs:
  - a `Hotkeys` entry in `features.json`
  - a wrapper in `UI_Hotkeys.bas`
  - a `CommandName`
  - a command class or mapped existing command
- If a feature can run unattended, set `"RuntimeTestMode": "headless"` in
  `features.json`.
- Interactive features should remain marked `"interactive"` so runtime smoke
  tests know their behavior.

## Build And Sync Workflow

Standard workflow:

1. Edit source files on disk.
2. Run `.\Update.ps1`.
3. Fix validation, import, compile, Ribbon, or runtime-test failures.

Useful commands:

```powershell
.\Update.ps1
.\Update.ps1 -SkipRuntimeTests
```

`Update.ps1` currently:

- validates Ribbon XML
- synchronizes `features.json` into `ribbon.xml`
- synchronizes `Hotkeys`, `Icons`, and `FeatureFlags` into `config.json`
- generates `Modules\Libraries\Lib_TestManifest.bas` from `Public Sub Test_*`
- verifies Ribbon callbacks exist in exported VBA
- enforces structure checks, `Option Explicit`, and metadata headers
- closes the workbook if Excel has it locked
- reimports managed VBA components from disk
- replaces `ThisWorkbook` from `ThisWorkbook.cls`
- compiles the VBA project through Excel/VBE automation
- saves the workbook
- injects `ribbon.xml` into the `.xlsm` archive as `customUI/customUI14.xml` and ensures the required relationship is registered in `_rels/.rels` (allowing the custom tab to appear in fresh or replaced workbooks)
- runs runtime smoke tests unless `-SkipRuntimeTests` is used
- bumps the patch/build version when `-BumpVersion` is passed

Prerequisites:

- Excel must allow trust access to the VBA project object model.
- The environment must support Excel COM automation.

## Current Development Directions

When making future changes, prefer these directions:

- extend the command architecture instead of adding direct feature logic to
  Ribbon or hotkey wrappers
- keep workbook events thin and route startup work through `Infra_Bootstrap`
- treat manifest metadata as the driver for UI, command routing, and test
  behavior
- consolidate reusable state, diagnostics, and config access in infrastructure
  modules instead of feature duplication
- keep new features compatible with runtime validation by choosing the right
  `RuntimeTestMode`

## Checklist

- Read the target module and its `@Dependencies` first.
- If touching Ribbon behavior, verify callback names and manifest entries
  match.
- If adding a command, update the manifest in `features.json`, then let
  `.\Update.ps1` regenerate `Infra_CommandRegistry`.
- If adding a config value, update both `Infra_ConfigModel` and `Infra_Config`.
- If it is a stable flag, also update `IConfig`.
- If mutating a range, consider `Infra_Undo.SaveState`.
- If the task is slow, add `Infra_Progress`.
- Run `.\Update.ps1` after meaningful changes.

## Known Gotchas

- Use `.Formula2`, not `.Formula`, for dynamic arrays and spill-safe formulas.
- `ThisWorkbook` now routes through `Infra_Bootstrap`, not directly to
  `Infra_Hotkeys`.
- `config.json` version values may be updated by `Update.ps1`; do not hand-
  manage patch increments.
- Ribbon XML changes require workbook reload or Excel restart to be reflected.
- `Infra_CommandRegistry` is generated from `features.json`. Keeping the generated 
  registry and the manifest aligned avoids drift.
- `Infra_AppStateGuard` should be declared inline as
  `Dim guard As New Infra_AppStateGuard` so cleanup runs on scope exit.
- `Infra_Error.Track` should be assigned to an object variable so RAII cleanup
  occurs correctly.

## Maintenance Note

When the project architecture changes, update this blueprint and `GEMINI.md`
together so the stable project facts and the agent workflow stay aligned.
